CREATE TABLE canvas(
    canvas TEXT,
    address TEXT,
    answer BOOLEAN,
    receptive INTEGER,
    comment TEXT,
    key INTEGER PRIMARY KEY AUTOINCREMENT,
    Timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
);